#ifdef TEACHER_HPP
#define TEACHER_HPP

#include "Person.hpp"
/**
* @brief Represents a `student` object
*/

class Teacher: public Person{
public:
  /**
  * @brief Default constructor for the Teacher class.
  */
  Teacher(); // Default constructor

  /**
   * @brief Destructor for the Teacher class.
   */
  ~Teacher();

private:
 
}

#endif // TEACHER_HPP
